TODO:

============parser.mly=================

- array and enum typing; possibly using a separate type from other variable
  declarations
- ocamlyacc won't recognize things defined in ast ( like Expr(...) )

==============ast.ml===================

- array and enum type (compound variable declaration type?)
- code generation / 'pretty printing' functions (?)


